org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true

# Redirect build output outside OneDrive to prevent file-lock issues during sync
# Flutter's top-level build dir is set via --build-dir flag; this covers Gradle intermediates
org.gradle.project.buildDir=C:/Users/duth/flutter_builds/android_app
